{
  "tasks": {
    "build": "gleam build --target=javascript && cp -r ./static ./build/dev/javascript/brettooo/dist",
    "run": "deno run --allow-net --allow-read --allow-env --watch ./build/dev/javascript/brettooo/dist/entry.js",
    "dev": "deno task build && deno task run"
  }
}